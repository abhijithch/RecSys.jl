Using RecSys.jl with MovieLens dataset.

- download dataset from <http://grouplens.org/datasets/movielens/>
- start `nworkers` Julia in parallel `julia -p <nworkers>`
- `include("movielens.jl")`
- run `test("path/to/dataset/ml-20m")`
